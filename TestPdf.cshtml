@using Modulewijzer.Models;
@using Modulewijzer.Converters;

@{
    var module = new Module()
    {
        Naam = "Moduletje",
        StudieJaar = 2,
        Periode = 2,
        AantalEcs = 1,
    };

    using (var pdfConverter = new PdfConverter(@""))
    {
        pdfConverter.AddDocent(new Docent() { Achternaam = "Test", Voorletters = "A. B. " });
        pdfConverter.AddModule(module);
    }
}