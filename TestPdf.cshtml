@using Modulewijzer.Models;
@using Modulewijzer.Converters;

@{
    var module = new Module()
    {
        Naam = "Moduletje",
        StudieJaar = 2,
        Periode = 2,
        AantalEcs = 1,
    };

    {
        pdfConverter.AddDocent(new Docent() { Achternaam = "Test", Voorletters = "A. B.", Tussenvoegsel= "de"});
        pdfConverter.AddModule(module);
    }
}